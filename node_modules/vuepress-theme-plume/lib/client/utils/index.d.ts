import { ResolvedNavItemWithLink } from '../../shared/index.js';

declare const EXTERNAL_URL_RE: RegExp;
declare const PATHNAME_PROTOCOL_RE: RegExp;
declare const HASH_RE: RegExp;
declare const EXT_RE: RegExp;
declare const inBrowser: boolean;
declare function toArray<T>(value: T | T[]): T[];
declare function isActive(currentPath: string, matchPath?: string, asRegex?: boolean): boolean;
declare function normalize(path: string): string;
declare function throttleAndDebounce(fn: () => void, delay: number): () => void;

declare function getCssValue(el: HTMLElement | null, property: string): number;
declare function getScrollTop(target?: Document | HTMLElement): number;
declare function setScrollTop(target?: Document | HTMLElement, scrollTop?: number): void;
declare function scrollTo(target: Document | HTMLElement, top: number, time?: number): void;

type RepoType = 'GitHub' | 'GitLab' | 'Gitee' | 'Bitbucket' | null;
declare function resolveRepoType(repo: string): RepoType;

declare const editLinkPatterns: Record<Exclude<RepoType, null>, string>;
declare function resolveEditLink({ docsRepo, docsBranch, docsDir, filePathRelative, editLinkPattern, }: {
    docsRepo: string;
    docsBranch: string;
    docsDir: string;
    filePathRelative: string | null;
    editLinkPattern?: string;
}): string | null;

/**
 * Resolve NavLink props from string
 *
 * @example
 * - Input: '/README.md'
 * - Output: { text: 'Home', link: '/' }
 */
declare function resolveNavLink(link: string): ResolvedNavItemWithLink;
declare function normalizeLink(base?: string, link?: string): string;
declare function normalizePrefix(base: string, link?: string): string;

/**
 * @method 缓动算法
 * t: current time（当前时间）；
 * b: beginning value（初始值）；
 * c: change in value（变化量）；
 * d: duration（持续时间）。
 */
declare function tween(t: number, b: number, c: number, d: number): number;
declare function linear(t: number, b: number, c: number, d: number): number;

export { EXTERNAL_URL_RE, EXT_RE, HASH_RE, PATHNAME_PROTOCOL_RE, type RepoType, editLinkPatterns, getCssValue, getScrollTop, inBrowser, isActive, linear, normalize, normalizeLink, normalizePrefix, resolveEditLink, resolveNavLink, resolveRepoType, scrollTo, setScrollTop, throttleAndDebounce, toArray, tween };
