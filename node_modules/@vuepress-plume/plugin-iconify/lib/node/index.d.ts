import { Plugin } from 'vuepress/core';

interface IconifyPluginOptions {
    componentName?: string;
    color?: string;
    size?: string | number;
}
declare function iconifyPlugin({ componentName, size, color, }?: IconifyPluginOptions): Plugin;

export { type IconifyPluginOptions, iconifyPlugin as default, iconifyPlugin };
